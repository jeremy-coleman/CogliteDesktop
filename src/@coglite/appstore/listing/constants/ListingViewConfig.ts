import { IListingViewConfig } from '../types';


const ListingViewConfig : IListingViewConfig = {
    label: "Listing",
    labelPlural: "Listings",
    storeLabel: "Store",
    bookmarksEnabled: false
};

export { ListingViewConfig }